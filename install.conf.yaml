- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link: 
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc: zshrc_omz
    ~/.zsh:
    ~/.shell:

- shell: [git submodule update --init --recursive, Installing submodules]
