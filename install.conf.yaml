- defaults:
    link:
      relink: true

- clean: ['~']

- link: 
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:

- shell: [git submodule update --init --recursive, Installing submodules]
